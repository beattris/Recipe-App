export class Ingredient {
    // public name: string;
    // public amount: number;

    // constructor(name: string, amount: number) {
    //     this.name = name;
    //     this.amount = amount;
    // }

    // THIS CODE BELOW IS A LIKE SHORTCUT TO ACHIEVING EXACTLY THE ABOVE CODE
    // WHEN THE PROPERTIES ARE JUST FEW
    
    constructor(public name: string, public amount: number){}
}